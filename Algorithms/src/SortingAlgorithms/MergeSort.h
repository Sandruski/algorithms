#pragma once

#include <vector>

namespace SortingAlgorithms
{
/**
 * Merge Sort
 * Time complexity: O(nlogn). Always the same
 */
namespace MergeSort
{
void Sort(std::vector<int>& inNums);
} // namespace MergeSort
} // namespace SortingAlgorithms
